name: Publish immutable backend release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing immutable annotated tag to recover
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: backend-release-${{ inputs.tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  publish-and-promote:
    runs-on: ubuntu-latest
    env:
      IMAGE: docker.io/dundunts/wage-app
    steps:
      - name: Check out complete source history
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - name: Validate immutable release tag and ancestry
        id: version
        env:
          RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-U[1-9][0-9]*)?$ ]]
          version=${RELEASE_TAG#v}
          verified_tag_ref="refs/release-tags/$RELEASE_TAG"
          git fetch --no-tags origin "refs/tags/$RELEASE_TAG:$verified_tag_ref"
          [[ "$(git cat-file -t "$verified_tag_ref")" == tag ]]
          source_sha=$(git rev-parse "$verified_tag_ref^{commit}")
          [[ "$source_sha" =~ ^[0-9a-f]{40}$ ]]
          release_version=${version%%-U*}
          git fetch --no-tags origin "refs/heads/release/$release_version:refs/remotes/origin/release/$release_version"
          git merge-base --is-ancestor "$source_sha" "origin/release/$release_version"
          git checkout --detach "$source_sha"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "release_version=$release_version" >> "$GITHUB_OUTPUT"
          echo "source_sha=$source_sha" >> "$GITHUB_OUTPUT"
      - name: Set up Java 21
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@4c125117fe7c5aed11272ec4213f602f012f89f2 # v5
      - name: Test release source once
        run: bash gradlew test --no-daemon
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3
      - name: Log in to Docker Hub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Refuse an existing version tag
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          SOURCE_SHA: ${{ steps.version.outputs.source_sha }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          bash scripts/verify-docker-tag.sh require-absent "$IMAGE" "$VERSION"
          bash scripts/verify-docker-tag.sh require-absent "$IMAGE" "git-$SOURCE_SHA"
      - name: Define immutable image metadata
        id: metadata
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=raw,value=${{ steps.version.outputs.version }}
            type=raw,value=git-${{ steps.version.outputs.source_sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ steps.version.outputs.source_sha }}
            org.opencontainers.image.version=${{ steps.version.outputs.version }}
      - name: Build and publish image once
        id: publish
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          platforms: linux/amd64
      - name: Verify published digest and tags
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          DIGEST: ${{ steps.publish.outputs.digest }}
          SOURCE_SHA: ${{ steps.version.outputs.source_sha }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          [[ "$DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]
          docker buildx imagetools inspect "$IMAGE@$DIGEST" >/dev/null
          bash scripts/verify-docker-tag.sh require-digest "$IMAGE" "$VERSION" "$DIGEST"
          bash scripts/verify-docker-tag.sh require-digest "$IMAGE" "git-$SOURCE_SHA" "$DIGEST"
      - name: Create narrowly scoped infrastructure token
        id: infra-token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.INFRA_APP_ID }}
          private-key: ${{ secrets.INFRA_APP_PRIVATE_KEY }}
          owner: dundunts
          repositories: wage-app-infr
          permission-contents: write
          permission-pull-requests: write
      - name: Check out infrastructure desired state
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: dundunts/wage-app-infr
          ref: main
          path: infra
          token: ${{ steps.infra-token.outputs.token }}
      - name: Open image-only infrastructure pull request
        env:
          GH_TOKEN: ${{ steps.infra-token.outputs.token }}
          DIGEST: ${{ steps.publish.outputs.digest }}
          SOURCE_SHA: ${{ steps.version.outputs.source_sha }}
          VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          cd infra
          [[ "$IMAGE" == "docker.io/dundunts/wage-app" ]]
          ruby -ryaml -e '
            path, version, digest = ARGV
            values = YAML.safe_load(File.read(path), permitted_classes: [], permitted_symbols: [], aliases: false)
            values.fetch("image")["version"] = version
            values.fetch("image")["digest"] = digest
            File.write(path, YAML.dump(values))
          ' environments/production/backend.yaml "$VERSION" "$DIGEST"
          branch="release/backend-$VERSION-$GITHUB_RUN_ID"
          git checkout -b "$branch"
          git config user.name "wage-app-release-promoter[bot]"
          git config user.email "wage-app-release-promoter[bot]@users.noreply.github.com"
          git add environments/production/backend.yaml
          git commit -m "Promote backend $VERSION"
          git push origin "$branch"
          body_file=$(mktemp)
          trap 'rm -f "$body_file"' EXIT
          printf '%s\n' \
            'Refs dundunts/wage-app-infr#8' \
            '' \
            'Automated immutable backend promotion.' \
            '' \
            "- Source: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/commit/$SOURCE_SHA" \
            "- Version: $VERSION" \
            "- Image: $IMAGE@$DIGEST" \
            '- Scope: environments/production/backend.yaml image.version + image.digest only' > "$body_file"
          gh pr create --repo dundunts/wage-app-infr --base main --head "$branch" \
            --title "Promote backend $VERSION" --body-file "$body_file"
